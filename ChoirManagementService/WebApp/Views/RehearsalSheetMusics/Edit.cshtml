@model Domain.App.RehearsalSheetMusic

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>RehearsalSheetMusic</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="RehearsalId" class="control-label"></label>
                <select asp-for="RehearsalId" class="form-control" asp-items="ViewBag.RehearsalId"></select>
                <span asp-validation-for="RehearsalId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SheetMusicId" class="control-label"></label>
                <select asp-for="SheetMusicId" class="form-control" asp-items="ViewBag.SheetMusicId"></select>
                <span asp-validation-for="SheetMusicId" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
